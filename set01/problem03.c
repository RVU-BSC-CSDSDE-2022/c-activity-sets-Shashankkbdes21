#include <stdio.h>
int input()
{
  int a;
  printf("Enter the value of a\n");
  scanf("%d",&a);
}
int find_sum(int a, int b)
{
  int sum;
  sum=a+b;
  return sum;
}
int void_output(int a,int b,int c);
{
  printf("The sum of %d+%d is %d");
}
int main(x,y,z)
{
  int (x,y,z);
  x = input();
  y = input();
  z = find_sum(x,y);(&x,&y,&z);
  return 0;
}
